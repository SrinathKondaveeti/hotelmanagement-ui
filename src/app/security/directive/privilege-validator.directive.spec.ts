import { PrivilegeValidatorDirective } from './privilege-validator.directive';

describe('PrivilegeValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new PrivilegeValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
